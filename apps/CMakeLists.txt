# set (PROG_NAME "dbgen3")
# set (MAIN_SRC main.cpp)

# add_executable(${PROG_NAME} ${MAIN_SRC})
# target_link_libraries (${PROG_NAME} glog::glog gflags)
# #   target_include_directories(${PROG_NAME}
# #   PUBLIC
# #     ${SUBDIRS}
# #   )

set (PROG_NAME "dbgen3")
